"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var MaskingUtils = /** @class */ (function () {
    function MaskingUtils() {
    }
    // Making the method static
    MaskingUtils.getReplacedHeaders = function (headers, maskHeaders) {
        if (headers) {
            var maskedHeaders = {};
            var _loop_1 = function (key) {
                if (maskHeaders && maskHeaders.length > 0) {
                    var foundKey = maskHeaders.find(function (headerName) { return headerName.toLowerCase() === key.toLowerCase(); });
                    if (foundKey) {
                        maskedHeaders[key] = "🔒MASKED🔒";
                        return "continue";
                    }
                }
                maskedHeaders[key] = headers[key];
            };
            for (var key in headers) {
                _loop_1(key);
            }
            return maskedHeaders;
        }
        return headers;
    };
    return MaskingUtils;
}());
exports.default = MaskingUtils;
