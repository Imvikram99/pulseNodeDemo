import { Express } from 'express';
import { Server } from '@hapi/hapi';
interface IOptions {
    app: Express | Server;
    url: string;
    applicationName: string;
    authKey: string;
    environment: string;
    teamName: string;
    partnerId: string;
}
export default class AppConfig {
    private static app;
    private static url;
    private static applicationName;
    private static authKey;
    private static environment;
    private static teamName;
    private static partnerId;
    static setConfig(options: IOptions): void;
    static getApp(): Express | Server;
    static getUrl(): string;
    static getApplicationName(): string;
    static getAuthKey(): string;
    static getEnvironment(): string;
    static getTeamName(): string;
    static getPartnerId(): string;
}
export {};
